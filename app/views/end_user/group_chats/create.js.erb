$('#group-chat-area').html('<%= j(render "index", chats: @chats) %>');
$('.text-area').val('');
$(document).ready(function() {
  const chatArea = $('#group-chat-area');
  chatArea.scrollTop(
    chatArea[0].scrollHeight - chatArea.height()
  );
});