div
  | 新規投稿
div
  = form_with model: @post do |f|
    = f.label :text
    = f.text_area :text
    = f.label :images
    = f.file_field :images, name: "post[images][]", multiple: true
    .form-check.form-switch
      = f.check_box :status, {class: "form-check-input"}, "published", "draft"
      = f.label :status, "公開する", class: "form-check-label"
    = f.label :name
    = f.text_field :name
    = f.submit "投稿する"
