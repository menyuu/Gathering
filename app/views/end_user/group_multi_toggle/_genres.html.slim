.errors
.card-body.mb-5.border-bottom
  | 現在のジャンル(最大8個まで)
  .my-3
    - group.genres.each do |genre|
      = link_to genre.name, group_destroy_genres_path(group, name: genre.name, model: "group"), method: :delete, remote: true, class: "btn btn-sm btn-dark rounded-pill tag-item"
.card-body.mb-5.border-bottom
  | 人気のジャンル
  .my-3
    - genres.each do |genre|
      = link_to genre.name, group_update_genres_path(group, name: genre.name, model: "group"), method: :patch, remote: true, class: "btn btn-sm btn-outline-dark rounded-pill tag-item"
.card-body.mb-5
  | ジャンルを追加する
  = form_with model: genre, url: group_create_genres_path, local: false do |f|
    .input-group
      = f.text_field :name, value: genre_names, placeholder: "複数のジャンルは半角のカンマ(,)で区切って入力してください。", class: "form-control input-group-prepend"
      = f.hidden_field :model, value: "group"
      span.input-group-append
        = f.submit "ジャンルを登録", class: "btn btn-outline-dark input-group"